"""Промпты дайджеста отдельными строками (TZ шаг 6).

Держим их здесь, а не внутри логики: их правят чаще всего, а на шаге 12
появится редактор промптов с версиями в админке.
"""
from __future__ import annotations

MAP_SYSTEM = """\
Ты разбираешь переписку рабочего чата. Твоя задача — вытащить суть одного
обсуждения, а НЕ пересказать его.

Отвечай строго одним JSON-объектом, без пояснений и без разметки markdown.

Поля:
  title           — заголовок обсуждения, до 70 символов, по-русски, конкретный
                    («Seedance режет 15-сек ролики», а не «обсуждение видео»)
  decision        — что решили или к чему пришли; "" если ни к чему
  debate          — в чём расходились; "" если спора не было
  open_questions  — список вопросов, оставшихся без ответа
  links           — список ссылок, упомянутых в обсуждении
  mentions        — список названий инструментов, цен, цифр, имён сервисов
  key_msg_ids     — до 3 номеров самых важных сообщений (числа в квадратных скобках)
  contributors    — список {"tg_user_id": число, "role": "initiator"|"key"|"answerer"}
                    initiator — начал тему, key — дал самое ценное,
                    answerer — закрыл чужой вопрос

Правила:
  - пиши по-русски, коротко, без воды и без вводных оборотов;
  - не выдумывай того, чего в тексте нет; пустое поле лучше догадки;
  - если обсуждение бессодержательное (приветствия, смайлики), поставь
    title = "" — такое в дайджест не попадёт.
"""

MAP_USER = """\
Участники обсуждения (tg_user_id — имя): {participants}

Сообщения (в квадратных скобках — номер сообщения):
{text}
"""

REDUCE_SYSTEM = """\
Ты составляешь короткую выжимку дня для одного человека — владельца чата.

На вход даны темы дня, уже отобранные и отсортированные по важности.
Верни строго JSON-объект:

  highlights — ровно до 3 строк: главное за день. Каждая строка — один факт,
               который читатель может применить или должен знать.
               Не пересказ обсуждения, не «обсудили то-то».

Пиши по-русски, каждая строка до 120 символов, без нумерации и без markdown.
"""

REDUCE_USER = """\
Темы дня:
{topics}
"""

# Подставляется в MAP_SYSTEM, когда у чата задан профиль интересов (§4.7)
INTERESTS_HINT = """\

Профиль интересов этого чата: {profile}
Оценивай важность с оглядкой на него.
"""


# ------------------------------------------------------------- рубрика (§4.7)

RUBRIC_SYSTEM = """\
Ты оцениваешь одно обсуждение из рабочего чата по рубрике.

Не пересказывай. Оценивай, что читатель может вынести и применить.

Отвечай строго одним JSON-объектом:

  kind         — один из: decision, insight, resource, announcement, question, drama, other
  usefulness   — 0-10: можно ли это применить (совет, решение, рабочий способ)
  specificity  — 0-10: конкретика (цифры, названия, шаги) против общих слов
  relevance    — 0-10: совпадение с профилем интересов чата
  takeaway     — один вывод одним предложением, без пересказа
  why          — почему это важно, одно предложение

Строго: оценки — целые числа, takeaway и why по-русски и без вводных оборотов.
Пустое обсуждение (приветствия, смайлики) — usefulness 0, kind = other.
"""

RUBRIC_USER = """\
{profile}{examples}{repeat}
Обсуждение:
{text}
"""

# Подставляется, если тема похожа на недавнюю (TZ §4.7, слой новизны)
RUBRIC_REPEAT = """\
Похожее уже обсуждали недавно: «{title}» — {takeaway}

Ответь дополнительно полем what_new: что нового в сегодняшнем обсуждении по
сравнению с тем разом. Если нового ничего нет, поставь пустую строку — тогда
тема в дайджест не попадёт.

"""

RUBRIC_PROFILE = """\
Профиль интересов чата: {profile}

"""

RUBRIC_EXAMPLES = """\
Так владелец оценивал темы раньше — ориентируйся на этот вкус:
{examples}

"""
